# AC-FR-01-01 TDD RED Work Export Report

| 항목 | 내용 |
|------|------|
| 프로젝트 | MagicSquare_XX |
| 문서 유형 | 작업 보고서 |
| 작업 요청 | Report 생성 + Prompt 폴더 Transcript Export |
| 작성일 | 2026-05-29 |
| 작업 범위 | AC-FR-01-01 테스트 설계 · RED 테스트 · 실행 환경 · 커버리지 HTML |

---

## 작업 목적

- FR-01 Boundary 입력 검증 중 **AC-FR-01-01**(4×4 형상)에 대한 테스트 계획·RED 테스트·실행 검증 결과를 `Report`에 기록한다.
- 동일 세션의 사용자 요청·AI 응답을 대화형 transcript로 `Prompt` 폴더에보낸다.

## 수행 결과

| ID | 작업 항목 | 결과 |
|----|-----------|------|
| R-01 | `Report/*.md` 기존 번호 확인 | 완료 (최대 `07`) |
| R-02 | `Prompt/*.md` 기존 번호 확인 | 완료 (최대 `07`) |
| R-03 | AC-FR-01-01 샘플 예제 선정 (Report 참고) | 완료 |
| R-04 | `docs/test_plan.md` 테스트 계획서 작성 | 완료 (이전 Turn) |
| R-05 | `README.md` RED 단계 To-Do 리스트 추가 | 완료 (이전 Turn) |
| R-06 | AC-FR-01-01 RED pytest 25건 작성 | 완료 |
| R-07 | `.venv` + `requirements-dev.txt` 구성 | 완료 |
| R-08 | pytest 실행 검증 | 완료 (20 failed, 10 passed — RED 정상) |
| R-09 | pytest-cov HTML 리포트 생성 | 완료 (`htmlcov/`, 전체 92%) |
| R-10 | `Report/08.AC-FR-01-01-TDD-RED-Work-Export-Report.md` 생성 | 완료 |
| R-11 | `Prompt/08.AC-FR-01-01-TDD-RED-Interactive-Transcript.md` 생성 | 완료 |

## 산출물

### 문서

- `Report/08.AC-FR-01-01-TDD-RED-Work-Export-Report.md`
- `Prompt/08.AC-FR-01-01-TDD-RED-Interactive-Transcript.md`
- `docs/test_plan.md` (TP-MS-001)
- `README.md` — § RED 단계 To-Do 리스트

### 코드·테스트·환경

| 경로 | 설명 |
|------|------|
| `tests/boundary/test_input_validator_ac_fr_01_01.py` | AC-FR-01-01 RED 테스트 25건 |
| `tests/boundary/conftest.py` | mock_resolver, in/out-of-scope fixture |
| `src/boundary/input_validator.py` | GREEN 대기 스텁 (`NotImplementedError`) |
| `src/boundary/schemas.py` | `ValidationErrorResponse` (pydantic) |
| `src/boundary/constants.py` | `INVALID_SIZE`, PRD 메시지 상수 |
| `src/control/magic_square_resolver.py` | `resolve()` 진입점 스텁 |
| `requirements-dev.txt` | pytest, pydantic, pytest-cov |
| `.venv/` | 로컬 가상환경 (gitignore) |
| `htmlcov/` | HTML 커버리지 리포트 (gitignore) |

---

## 핵심 결정 사항

### AC-FR-01-01 샘플 예제 (테스트 플랜·RED 공통)

| 항목 | 값 |
|------|-----|
| AC ID | **AC-FR-01-01** |
| PRD | **FR-01**, **§8.1** |
| 시나리오 | **SC-BND-VAL-004** (Level 5 보강 제안) |
| 입력 | `grid = None` |
| 기대 | `{ code: "INVALID_SIZE", message: "Grid must be 4x4." }` |
| 격리 | 검증 실패 시 Domain `resolve()` **0회** |

### RED 테스트 구성 (25건)

| 클래스 | 건수 | 역할 |
|--------|------|------|
| `TestAcFr0101NormalFailureReturn` | 5 | `None` → 실패 DTO |
| `TestAcFr0101BoundaryValues` | 5 | `[]`, `[[]]*4`, 3×4, 4×3 |
| `TestAcFr0101ResolverIsolation` | 5 | `handle_input` 후 resolve 미호출 |
| `TestAcFr0101MessageExactMatch` | 5 | PRD §8.1 문구·코드 일치 |
| `TestAcFr0101ScopeLimitation` | 5 | AC-FR-01-02~05 fixture 제외 확인 |

---

## pytest 실행 결과 (최종)

| 실행 | 명령 요약 | 결과 |
|------|-----------|------|
| 전체 테스트 | `pytest tests -v` | **20 failed, 10 passed** |
| 실패 원인 | `InputValidator` 미구현 | `NotImplementedError` (RED) |
| 통과 10건 | Scope 5 + `test_user` 5 | 구현·fixture 메타 검증 |

## 커버리지 (pytest-cov HTML)

| 항목 | 값 |
|------|-----|
| 명령 | `pytest tests --cov=src --cov-report=html --cov-report=term-missing` |
| 출력 | `htmlcov/index.html` |
| TOTAL | **92%** (51 stmts, 4 miss) |
| Boundary | 100% (스텁 라인만 실행 — GREEN 후 의미 재평가 필요) |
| Entity `user.py` | 86% |
| Control `resolve` | 75% (미호출) |

> RED 단계에서 Boundary 100%는 실제 검증 로직 커버가 아니라 예외 raise 경로만 탄 것으로 해석한다.

---

## 검증 체크리스트

- [x] 보고서가 `Report` 경로에 신규 번호 `08`로 생성됨
- [x] transcript가 `Prompt` 경로에 생성됨 (`08`번)
- [x] 기존 Report `07` 덮어쓰기 없음
- [x] transcript에 User/AI Turn 구분 포함
- [x] RED 상태(20 fail) 문서화됨

---

## 남은 작업 (GREEN 권장 순서)

1. `InputValidator.validate_grid` — 형상 검사 (`None`, 행/열 ≠ 4)
2. `InputValidator.handle_input` — 실패 시 `resolve()` 미호출
3. `pytest tests/boundary/test_input_validator_ac_fr_01_01.py` 전건 통과
4. README RED To-Do Track A/B 체크
5. Boundary 커버리지 85%+ 재측정 (실제 분기 기준)

---

## 비고

- Transcript는 `Prompt/08`에 저장한다. (초기에는 `Prompting/01`에 두었다가 `Prompt/08`로 이동)
- 다음 Export 시 Report `09`, Prompt `09`부터 이어가면 된다.

---

## 문서 이력

| 버전 | 일자 | 내용 |
|------|------|------|
| 1.0 | 2026-05-29 | AC-FR-01-01 TDD RED 작업 Export 초판 |
